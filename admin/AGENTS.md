# Admin Agents Guide

本目录当前是预留工作区，详细说明见 `admin/CLAUDE.md`。

## Agent Rules

- 把 `admin/` 视为未初始化目录，不要假设这里已经存在可运行项目
- 不要编辑 `admin/.setting.zhd/` 中的日志、缓存或工具状态文件
- 新增文档优先放根目录 `docs/`，新增脚本优先放根目录 `scripts/`
- 如需在本目录开始搭建后台项目，先明确技术栈和目标，再批量创建文件
