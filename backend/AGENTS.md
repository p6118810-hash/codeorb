# Backend Agents Guide

本目录当前是预留后端工作区，详细说明见 `backend/CLAUDE.md`。

## Agent Rules

- 不要把 `backend/` 当成已初始化的服务项目
- 不要改动 `backend/.setting.zhd/` 中的工具状态文件
- 如果开始搭建后端，请优先明确职责边界：API、同步任务、鉴权或 webhook
- 共享脚本与文档优先放在根目录 `scripts/` 和 `docs/`
